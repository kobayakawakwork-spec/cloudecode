# CLAUDE.md

このリポジトリは、Claude Codeを使ってデザイン性・UI/UX・コンバージョン性能の高い **Webサイト / コーポレートサイト / サービスサイト / LP** を制作するためのベース環境です。
今後のすべてのWeb制作タスクにおいて、以下のルールを常に適用してください。

## 1. 制作方針

単に「整ったWebサイト」を作ることを目標にしない。プロのWebデザイナー・アートディレクター・フロントエンドエンジニアが共同制作した水準を目指す。

特に「AIが生成したようなテンプレート感」を避けること。以下の表現を安易に多用しない。

- 紫や青のグラデーション
- 過剰な角丸カード
- すべての要素をカード化するレイアウト
- 意味のないグロー表現
- 不必要なGlassmorphism
- 単調な中央揃え
- 同じ構成のセクションの連続
- ありがちなAI/SaaS風デザイン
- 過剰な絵文字やアイコン
- デザイン上の意味がない装飾

案件ごとにターゲット・ブランド・商材・目的を理解し、案件ごとに異なるアートディレクションを設計すること。

## 2. 制作前に必ず設計する

実装を始める前に、以下を必ず整理する。デザインコンセプトを決めずに、いきなりコードを書き始めない。

1. Webサイトの目的
2. ターゲットユーザー
3. コンバージョンポイント
4. ブランドイメージ
5. 競合との差別化
6. 情報設計
7. デザインコンセプト
8. 配色
9. タイポグラフィ
10. レイアウトシステム
11. 写真・イラストの方向性
12. モーションの方向性

## 3. Skillの利用

Web制作では、必要に応じてインストール済みSkillを積極的に使用する。

- **frontend-design**: 新規デザイン制作・大幅なUI改善で優先的に使用する。アートディレクション、レイアウト、タイポグラフィ、配色、ビジュアル表現の品質向上に利用する。
- **shadcn**: フォーム、ダイアログ、ナビゲーション、Accordion、Tabsなど、UIコンポーネントが必要な場合に利用する。ただし、shadcnのデフォルトデザインをそのまま並べるだけのサイトにはしない。ブランドに合わせてカスタマイズする。
- **web-design-guidelines**: 実装後のUI/UXレビューに使用する。アクセシビリティ、操作性、フォーム、レスポンシブ、タイポグラフィ、画像、インタラクションなどを確認する。
- **vercel-react-best-practices**: ReactまたはNext.jsを使用する場合、最終工程で必ず確認する。パフォーマンス、レンダリング、バンドルサイズ、コンポーネント設計などを最適化する。

## 4. Figma MCP

Figmaデザインが提供された場合は、Figma MCPを使用する。スクリーンショットだけで判断せず、可能な限りFigmaの構造、コンポーネント、Auto Layout、Variables、Spacing、Typographyなどの情報を取得する。

Figmaを参考に実装する場合は、デザインを可能な限り忠実に再現する。ただしレスポンシブ時の挙動など、Figmaに定義されていない部分についてはUI/UX上適切な判断をする。

## 5. 参考サイト

参考サイトや参考URLが指定された場合、そのデザインをそのままコピーしない。以下を分析する。

- 情報設計
- ファーストビュー
- グリッド
- 余白
- フォントサイズ
- 写真の使い方
- CTA
- ナビゲーション
- モーション
- セクション構成
- ブランド表現

分析した原則を、新しいブランドに合わせて再構築する。

## 6. LP制作

LPでは見た目だけでなくコンバージョンを重視する。特に以下を確認する。

- 広告とファーストビューのメッセージ一致
- ターゲットが数秒で「自分向け」と理解できるか
- CTAの視認性
- CTA直前の心理的障壁
- ベネフィット
- 信頼性
- 実績・社会的証明
- FAQ
- フォーム離脱
- モバイルでのCTA
- 情報量とスクロール負荷

デザイン性を高めるためにCVRを犠牲にしない。

## 7. レスポンシブ

モバイルを後付けにしない。Desktop / Tablet / Mobileそれぞれについて、以下を設計する。

- 情報の優先順位
- 余白
- フォントサイズ
- CTA
- ナビゲーション
- 画像トリミング
- タップ領域

特にLPはスマートフォンでの完成度を重視する。

## 8. モーション

モーションは装飾ではなく、ユーザー体験を向上させる目的で使用する。必要に応じて以下を利用できる。

- Scroll animation
- Hover
- Reveal
- Parallax
- Page transition
- Micro interaction

ただし過剰なアニメーションによって、表示速度や可読性、CVRを悪化させない。`prefers-reduced-motion` にも配慮する。

## 9. パフォーマンス

高品質なデザインと高速表示を両立する。特に以下を意識する。

- LCP
- CLS
- INP
- 画像最適化
- Web Font
- JavaScript量
- Lazy Load
- レンダリング

## 10. 完成前の品質確認

実装完了後、そのまま完成扱いにしない。以下の順番でセルフレビューを行う。

1. デザイン品質
2. ブランドとの整合性
3. Desktop表示
4. Mobile表示
5. CTA・導線
6. UI/UX
7. アクセシビリティ
8. パフォーマンス
9. コード品質
10. 表示崩れ

利用可能な場合はChrome/browser toolsを使って実際のページを確認し、スクリーンショットも確認する。問題があれば修正 → 再確認を行う。

## 11. 最重要ルール

「動くものができた」ことを完成条件にしない。完成条件は、

> デザイン性・ブランド表現・UI/UX・レスポンシブ・コンバージョン・パフォーマンス・実装品質が高いレベルで成立していること

である。常に「有名なWeb制作会社や優秀なデザイナーが制作した場合、この品質で納品するか？」という基準でセルフレビューすること。
